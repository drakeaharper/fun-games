# `npm start` talks to the local Cloudflare Worker (`npm run dev` in ../worker).
# Production builds omit these and use same-origin URLs.
REACT_APP_API_URL=http://localhost:8787/api
REACT_APP_WEBSOCKET_URL=ws://localhost:8787
